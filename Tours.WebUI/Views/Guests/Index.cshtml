@model IEnumerable<SessionModel.Guest>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>لیست مهمانان جلسه <text>@TempData["MeetingName"]</text></h2>


<form id="Save" name="save" method="post">
    <table class="table table-hover table-bordered table-stripped">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Family)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Post)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NationalCode)
            </th>
           
            <th>
                @Html.DisplayNameFor(model => model.In_Out)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrganId)
            </th>
            <th>
                عملیات
            </th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Family)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Post)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NationalCode)
                </td>
                @*<td>
                        @Html.DisplayFor(modelItem => item.MeetingId)
                    </td>*@
                <td>
                    @Html.DisplayFor(modelItem => item.In_Out)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.OrganId)
                    @*@Html.DropDownListFor(modelItem => item.OrganId, null, new { @class = "form-control" })*@
                </td>

                <td>

                    <a href="@Url.Action("Delete", new { id = item.Id })" class="btn btn-danger btn-xs">
                        <span class="glyphicon glyphicon-remove" title="حذف"></span>
                    </a>
                    <a href="@Url.Action("Edit", new { id = item.Id })" class="btn btn-primary btn-xs">
                        <span class="glyphicon glyphicon-pencil" title="ویرایش"></span>
                    </a>
                    <a href="@Url.Action("Create2", new { MeetingId = ViewBag.MeetingId,id=item.Id,name=item.Name,family=item.Family,post=item.Post,nationalcode=item.NationalCode,in_out=item.In_Out,organid=item.OrganId })" class="btn btn-success btn-xs">
                        <span class="glyphicon glyphicon-plus" title="افزودی به جلسه: @TempData["MeetingName"]"></span>
                    </a>
                </td>
            </tr>

        }
    </table>



            <div  >
                <a href="@Url.Action("CreateGuest","Meeting", new {MeetingId = ViewBag.MeetingId})" class="btn btn-success btn-lg">
                    <span>تعریف مهمان جدید</span>
                </a>
            </div>
   





    @*<div>
        <p>
            <a href="@Url.Action("Create2",new { MeetingId = TempData["meetingId"] })" class="btn btn-success btn-sm pull-left">افزودن مهمان</a>
        </p>
    </div>*@
</form>

